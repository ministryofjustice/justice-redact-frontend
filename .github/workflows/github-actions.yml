name: 'Build, Push & Deploy'

on:
  push:
    branches: [main]

jobs:
  ecr_and_deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Required for AWS OIDC authentication (Code 1 style)
      contents: read  # Required for actions/checkout
    
    env:
      KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}

    steps:
      # 1. Checkout GitHub repository
      - uses: actions/checkout@v3

      # 2. Configure AWS credentials using OIDC (Aligned with Code 1)
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.ECR_ROLE_TO_ASSUME }}
          aws-region: ${{ vars.ECR_REGION }}

      # 3. Login to container repository
      - name: Log into ECR
        uses: aws-actions/amazon-ecr-login@v1
        id: login-ecr

      # 4. Build and push a Docker image (Using Code 1's node tag pattern)
      - name: Build & Push to ECR
        run: |
          docker build -t $REGISTRY/$REPOSITORY:node-$IMAGE_TAG .
          docker push $REGISTRY/$REPOSITORY:node-$IMAGE_TAG
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }} # Dynamically grabs registry like Code 1
          REPOSITORY: ${{ vars.ECR_REPOSITORY }}
          IMAGE_TAG: ${{ github.sha }}

      # 5. Generate Kubernetes manifests
      - name: Generate Kubernetes manifests
        run: |
          sudo apt-get update && sudo apt-get install -y gettext
          mkdir -p k8s

          export IMAGE=$REGISTRY/$REPOSITORY:node-$IMAGE_TAG
          export TEAM_NAME=justice-redact
          export ENVIRONMENT=dev
          export SERVICE_ACCOUNT="${TEAM_NAME}-${ENVIRONMENT}"

          envsubst < deployments/templates/deployment.yml > k8s/deployment.yml
          envsubst < deployments/templates/service.yml > k8s/service.yml
          envsubst < deployments/templates/ingress.yml > k8s/ingress.yml
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          REPOSITORY: ${{ vars.ECR_REPOSITORY }}
          IMAGE_TAG: ${{ github.sha }}

      # 6. ✅ FIX: Authenticate using Kube Token instead of 'aws eks update-kubeconfig'
      - name: 'Authenticate to the cluster'
        env:
          KUBE_CERT: ${{ secrets.KUBE_CERT }}
          KUBE_TOKEN: ${{ secrets.KUBE_TOKEN }}
          KUBE_CLUSTER: ${{ secrets.KUBE_CLUSTER }}
        run: |
          echo "${KUBE_CERT}" > ca.crt
          kubectl config set-cluster ${KUBE_CLUSTER} --certificate-authority=./ca.crt --server=https://${KUBE_CLUSTER}
          kubectl config set-credentials deploy-user --token=${KUBE_TOKEN}
          kubectl config set-context ${KUBE_CLUSTER} --cluster=${KUBE_CLUSTER} --user=deploy-user --namespace=${KUBE_NAMESPACE}
          kubectl config use-context ${KUBE_CLUSTER}

      # 7. Verify cluster access
      - name: Verify cluster access
        run: |
          kubectl get pods -n ${KUBE_NAMESPACE}

      # 8. Deploy to Kubernetes
      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f k8s/ -n ${KUBE_NAMESPACE}
